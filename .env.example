# The browser must use the same-origin /api/domain proxy so HttpOnly sessions work
# and no cross-origin Supabase CORS preflight is required.
VITE_DOMAIN_PROXY_BASE=/api/domain

# Configure these as server-side deployment variables (do not expose them through VITE_*).
SUPABASE_FUNCTIONS_BASE=https://igihzeyfgwhnuiflamvn.supabase.co/functions/v1
SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_me
